{
  "name": "wp-desktop-backup",
  "version": "1.0.0",
  "main": "backend/main.js",
  "devDependencies": {
    "bower": "^1.4.1",
    "electron-prebuilt": "^0.30.1",
    "electron-rebuild": "^0.2.5",
    "electron-packager": "^5.0.0"
  },
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . WordpressDesktopBackup --platform=all --arch=x64 --version=0.30.1 --out=dist --overwrite --ignore=node_modules --ignore='material-design-iconic-font/svg' --ignore=dist"
  },
  "dependencies": {
    "async": "^1.4.0",
    "fs-jetpack": "^0.7.0",
    "moment": "^2.10.6",
    "q": "^1.4.1",
    "request": "^2.60.0",
    "superagent": "^1.2.0"
  }
}
